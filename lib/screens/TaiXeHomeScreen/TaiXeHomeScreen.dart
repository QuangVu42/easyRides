import 'package:flutter/material.dart';

class TaiXeHomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Tài xế Dashboard")),
      body: Center(child: Text("Chào mừng Tài xế")),
    );
  }
}